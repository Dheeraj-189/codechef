#include <stdio.h>

int main() {
    int n, i = 1, sum = 0;

    // Read input
    scanf("%d", &n);

    // Loop from 1 to n
    while (i <= n) {
        if (i % 2 == 0) {
            sum = sum + i;
        }
        i++;
    }

    // Print the result
    printf("%d", sum);

    return 0;
}
